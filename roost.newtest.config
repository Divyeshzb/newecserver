build:
  type: bash
  bash: |-
    

    docker build -f ecserver/Dockerfile -t ecserver .
    cd ./ && make all
    cd ./ && make all
deploy:
  type: bash
  bash: "#!/bin/sh"
test:
  - ? name
    ? configType
    ? configPath
