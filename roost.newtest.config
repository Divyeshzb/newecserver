build:
  type: text
  bash: |-
    

    docker build -f ecserver/Dockerfile -t ecserver .
    cd ./ && make all
    cd ./ && make all
deploy:
  type: text
  bash: "#!/bin/sh"
test:
  - ? name
    ? configType
    ? configPath
